apiVersion: apps/v1
kind: Deployment

metadata:
  name: app-service-mesh-v2

spec:
  replicas: 4
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 20%
      maxUnavailable: 10%
  selector:
    matchLabels:
      app: app-service-mesh
  template:
    metadata:
      labels:
        app: app-service-mesh
        version: v2
    spec:
      containers:
        - name: app-service-mesh
          image: mdta023/app-service-mesh:v2
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3333
          resources:
            requests:
              cpu: 200m
              memory: 128Mi
            limits:
              cpu: 400m
              memory: 256Mi
